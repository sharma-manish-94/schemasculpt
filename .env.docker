# SchemaSculpt Docker Compose Environment Variables
# This file contains default values for Docker Compose services
# For local overrides, create .env.docker.local (gitignored)

# ============================================================================
# INFRASTRUCTURE SERVICES (Always active)
# ============================================================================

# PostgreSQL Configuration
DB_PASSWORD=postgres
# POSTGRES_DB=schemasculpt
# POSTGRES_USER=postgres
# POSTGRES_PORT=5432

# Redis Configuration
# REDIS_PORT=6379
# REDIS_PASSWORD=  # Uncomment for production

# ============================================================================
# APPLICATION SERVICES (Only with --profile full)
# ============================================================================

# Ollama Configuration (runs on host, not in Docker)
OLLAMA_BASE_URL=http://host.docker.internal:11434
MODEL_NAME=mistral:7b-instruct

# AI Service Configuration
# AI_SERVICE_PORT=8000

# API Gateway Configuration
# API_PORT=8080
# SPRING_PROFILES_ACTIVE=prod

# GitHub OAuth (optional - for authentication features)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# JWT Secret (change in production!)
JWT_SECRET=change-me-in-production

# UI Configuration
# UI_PORT=3000
# REACT_APP_API_URL=http://localhost:8080
