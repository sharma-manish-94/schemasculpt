# =====================================================================
# SchemaSculpt AI Service Dependencies - FIXED VERSION
# =====================================================================
# Use this file if you encounter installation issues with requirements.txt

# ---------------------------------------------------------------------
# Core Web Framework
# ---------------------------------------------------------------------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ---------------------------------------------------------------------
# HTTP Clients
# ---------------------------------------------------------------------
httpx>=0.25.0
requests>=2.31.0

# ---------------------------------------------------------------------
# OpenAPI Spec Processing
# ---------------------------------------------------------------------
prance>=23.6.0
openapi-spec-validator>=0.7.0

# ---------------------------------------------------------------------
# Configuration & Environment
# ---------------------------------------------------------------------
python-dotenv>=1.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# ---------------------------------------------------------------------
# Data Formats
# ---------------------------------------------------------------------
PyYAML>=6.0.1

# ---------------------------------------------------------------------
# JSON Operations
# ---------------------------------------------------------------------
jsonpatch>=1.33
jsonschema>=4.19.0

# ---------------------------------------------------------------------
# RAG and Vector Search (Required for AI Explanation System)
# ---------------------------------------------------------------------
langchain>=0.1.0
langchain-community>=0.0.20
langchain-huggingface>=0.0.1
langchain-text-splitters>=0.1.0
chromadb>=0.4.22
sentence-transformers>=2.2.0
pypdf>=3.17.0
unstructured>=0.11.0
lark>=1.1.0
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# ---------------------------------------------------------------------
# LLM Provider Dependencies (Optional but recommended)
# ---------------------------------------------------------------------
# Uncomment if you want to use HuggingFace models directly
# torch>=2.1.0; platform_machine != "aarch64"
# transformers>=4.35.0

# ---------------------------------------------------------------------
# System Monitoring
# ---------------------------------------------------------------------
psutil>=5.9.0

# ---------------------------------------------------------------------
# MCP (Model Context Protocol) Integration - FIXED
# ---------------------------------------------------------------------
# The MCP SDK requires specific installation from GitHub
# Install separately with: pip install mcp --pre
# Or install from source: pip install git+https://github.com/modelcontextprotocol/python-sdk.git

# Note: MCP is optional - repository features will be disabled if not available
# To enable repository features:
# 1. Install Node.js and npm
# 2. Install MCP servers: npm install -g @modelcontextprotocol/server-github
# 3. Install Python MCP SDK: pip install mcp --pre

aiofiles>=23.2.1  # Required for async file operations (used by MCP)
