# =====================================================================
# SchemaSculpt AI Service Dependencies
# =====================================================================

# ---------------------------------------------------------------------
# Core Web Framework
# ---------------------------------------------------------------------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ---------------------------------------------------------------------
# HTTP Clients
# ---------------------------------------------------------------------
httpx>=0.25.0  # Async HTTP client (used by all providers)
requests>=2.31.0  # Sync HTTP client (fallback)

# ---------------------------------------------------------------------
# OpenAPI Spec Processing
# ---------------------------------------------------------------------
prance>=23.6.0
openapi-spec-validator>=0.7.0

# ---------------------------------------------------------------------
# Configuration & Environment
# ---------------------------------------------------------------------
python-dotenv>=1.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# ---------------------------------------------------------------------
# Data Formats
# ---------------------------------------------------------------------
PyYAML>=6.0.1

# ---------------------------------------------------------------------
# JSON Operations
# ---------------------------------------------------------------------
jsonpatch>=1.33
jsonschema>=4.19.0

# ---------------------------------------------------------------------
# RAG and Vector Search (Optional - for security analysis)
# Install if using RAG features:
# pip install "schemasculpt-ai[rag]"
# ---------------------------------------------------------------------
langchain>=0.1.0
langchain-community>=0.0.20
langchain-huggingface>=0.0.1
chromadb>=0.4.0
sentence-transformers>=2.2.0
pypdf>=3.17.0
unstructured>=0.11.0
lark>=1.1.0
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# ---------------------------------------------------------------------
# LLM Provider Dependencies
# ---------------------------------------------------------------------

# For HuggingFace Provider (Optional)
# Install if using HuggingFace:
# pip install "schemasculpt-ai[huggingface]"
torch>=2.9.0; platform_machine != "aarch64"  # GPU acceleration
transformers>=4.35.0  # HuggingFace transformers

# ---------------------------------------------------------------------
# System Monitoring (Optional)
# ---------------------------------------------------------------------
psutil>=5.9.0

# ---------------------------------------------------------------------
# MCP (Model Context Protocol) Integration
# ---------------------------------------------------------------------
mcp>=0.9.0  # MCP SDK for Python
aiofiles>=23.2.1  # Async file operations for MCP
marshmallow>=3.26.2 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
