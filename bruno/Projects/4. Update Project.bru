meta {
  name: Update Project
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/projects/{{projectId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Petstore API v2",
    "description": "Updated OpenAPI specification for a pet store",
    "isPublic": true
  }
}

docs {
  # Update Project

  Updates project details.
  All fields are optional - only provided fields will be updated.
}

assert {
  res.status: eq 200
}
